name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: fastai/workflows/nbdev3-ci@master
        with:
          version: '3.11'
          pre: |
            echo "${{ secrets.CONFIG_HYPERLIQUID_JSON }}" > config_hyperliquid.json